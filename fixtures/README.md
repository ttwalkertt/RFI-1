# Fixtures

Checked-in deterministic fixtures used by later tests belong here. TASK-001 deliberately provides
no acquisition or domain fixtures.

Machine-local or downloaded fixtures belong under `fixtures/local/` and are ignored.
