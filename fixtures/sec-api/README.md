# Sanitized SEC-API.io transport fixtures

These small fixtures reproduce only the official Query API fields and complete-submission shape
needed by the adapter tests. They are synthetic, deterministic, and structurally based on the
public SEC-API.io documentation because no live credential was available during TASK-004. They
contain no credential, authorization header, account identifier, quota value, private URL, or
live response body. They are not evidence of a provider-backed run.
